# to have mpi enabled
source /etc/profile.d/modules.sh
module load mpi
# to be able to run python installed executables
export PATH=/home/casauser/.local/bin:$PATH
#which is not standard!
which() { type -P "$@"; }
export -f which
